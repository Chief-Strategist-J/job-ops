services:
  - type: web
    name: job-ops
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 3001
      - key: DATA_DIR
        value: /app/data
      - key: NODE_ENV
        value: production
